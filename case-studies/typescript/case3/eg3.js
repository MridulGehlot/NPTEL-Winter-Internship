var city = "Ujjain";
var temperature = 45.1;
var isRaining = false;
function weatherReport(city, temperature, isRaining) {
    console.log("In ".concat(city, ", it is ").concat(temperature, " degree celsius. Is it raining ? ").concat(isRaining));
}
weatherReport(city, temperature, isRaining);
